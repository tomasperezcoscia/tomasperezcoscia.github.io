import style from '../style'
import {goodProfile2} from '../assets/'

const Hero = () => (
    <section id='hero'>
      <img src={goodProfile2} alt="profilePicture" />
    </section>
  )

export default Hero